<div class="wrapper">
  <div class="row-fluid content">
    <%=raw @page.content %>
  </div>
</div>
<%= render "shared/portfolio" if @page.slug == 'index' %>
<%= render "shared/mailer/feedback" if @page.slug == 'contacts' %>
